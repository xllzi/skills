{
  "eval-rewrite-slop-readme": "with_skill (new): lint clean, purpose kept, no inventions — the targeted fixes worked. Blind judge still prefers old_skill narrowly: the rewrite 'mirrors the source clause by clause in a mechanical way'; old_skill's restructured version (headings, direct 'You get one view') reads more human. Both pass all 9 assertions.",
  "eval-write-pr-description": "with_skill (new): fabrication eliminated — every unknown is a placeholder. But the blind judge rejects the result as a deliverable: 'ships with unfilled placeholders, so it never delivers an actual PR description'. The no-invention rule over-corrected for one-shot documents. Also '<failure count>' is reused for both before and after runs. old_skill passes 6/7 but fabricates 4 attempts, 100/200/400/800ms, 20% jitter, 6h soak, 4.8%->0.02%, 340ms p99.",
  "eval-strict-error-messages": "with_skill (new): placeholders + fill explanations exactly as the new rule requires; 4 D01 all the documented file(noun) false positive. Judge calls the per-message glosses 'unrequested content that bloats a document meant to hold error text'. old_skill is terminal-ready and D01-clean but hardcodes config.yaml/line 12/7200/3600 for the second iteration running — stable failure mode."
}
